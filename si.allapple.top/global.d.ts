// Removed: temporary global type stubs are no longer needed because
// `@types/react`, `@types/react-dom`, and `@types/node` are installed.
// If you still see TypeScript errors, run:
//   D:\nodee\npm.cmd install -D @types/react @types/react-dom @types/node
// Then restart your editor/TS server.
